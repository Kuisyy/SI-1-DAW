Realizar un script que dado un directorio, cree un archivo tar comprimido con gzip y con nombre igual a la fecha en formato yyyy-mm-dd seguido del nombre del directorio acabado en .tar.gz. Ejemplo: aplicado sobre la carpeta tmp obtendríamos 2014-04031.tmp.tar.gz
Realizar un script que dado un número indique si es o no divisible entre 101. Si no se proporciona un número debe mostrar cómo usar el programa.
Realziar un script que permita adivinar el usuario cual es su PID. El script pide un numero la usuario y cada vez que lo haga debe indicar al usuario si el PID es mayor o menor que el número introducido.Cuando se adivine el valor,se deben mostrar los intentos empleados.
