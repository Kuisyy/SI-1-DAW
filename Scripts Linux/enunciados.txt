Guion de Shell que genere un fichero llamado listaetc que contenga los ficheros con permiso de lectura que haya en el directurio/etc
Hace un guion de shell que, partiendo del fichero generado en el fichero anteriro, muestre todos los ficheros del directorio/etc que contenga la palabra ¨procmail¨ (u otra cualquier de vuestra eleccion)
Hacer un guion de shell que cuente cuantos ficheros y cuantos directorios hay en el directorio pasado como argumento
Hacer un guion de Shell que compruebe si existe el directorio pasado como argumento dentro del directorio activo. En caso de que exista, que diga si no esta vacio
Hacer un guion de Shell que copie rodos los ficheros del directorio actual en un directorio llamado csg.Si el directorio no existe el guion lo debe de crear
Hacer un script que muestre el fichero dek directorio activo con mas lineas.